import {useState} from "react";
import {useNavigate,Link} from "react-router-dom";
import api from "../api";
export default function Register(){
 const [form,setForm]=useState({name:"",mobile:"",email:"",password:""}),[error,setError]=useState(""),[loading,setLoading]=useState(false); const nav=useNavigate();
 const submit=async e=>{e.preventDefault();setError("");setLoading(true);try{const r=await api.post("/vendors/register",form);localStorage.setItem("digitalvendor_token",r.data.token);nav("/shop-setup")}catch(e){setError(e.response?.data?.message||"Registration failed")}finally{setLoading(false)}};
 return <main className="auth-page"><div className="auth-card wide"><div className="text-center mb-4"><div className="brand fs-3">Digital<span>Vendor</span></div><h3 className="fw-bold mt-3">Create your vendor account</h3><p className="text-secondary">Start bringing your shop online.</p></div>{error&&<div className="alert alert-danger">{error}</div>}<form onSubmit={submit}><label>Name</label><input className="form-control mb-3" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/><label>Mobile Number</label><input className="form-control mb-3" value={form.mobile} onChange={e=>setForm({...form,mobile:e.target.value})} required/><label>Email <small>(optional)</small></label><input type="email" className="form-control mb-3" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><label>Password</label><input type="password" minLength="6" className="form-control mb-4" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/><button className="btn btn-primary w-100" disabled={loading}>{loading?"Creating...":"Create Vendor Account"}</button></form><p className="text-center mt-3">Already registered? <Link to="/login">Login</Link></p></div></main>
}
